import { PhasePlaceholderPage } from "@/components/phase-placeholder-page";

export default function InventoryPage() {
  return (
    <PhasePlaceholderPage
      title="Inventory"
      description="Transaction-based inventory begins in Phase 3. The Phase 1 foundation preserves the ledger-first architecture."
    />
  );
}
